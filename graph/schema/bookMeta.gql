
extend type Query {
  bookMeta(id: String!): BookMetadata,
  bookMetas: [BookMetadata!]!,
}

extend type Mutation {
  createBookMeta(inp: CreateBookMetaInp!): BookMetadata,
  removeBookMeta(id: String!): String,
  updateBookMeta(inp: UpdateBookMetaInp!): BookMetadata,
}

type BookMetadata {
  id: String!,
  name: String!,
  abstract: String!,
  ISBN: String!,
  authors: [Author!]!,
  publisherID: String!,
  publisher: Publisher!,
  books: [Book!]!,
}

input CreateBookMetaInp {
  name: String!,
  abstract: String!,
  ISBN: String!,
  authorIDs: [String!]!,
  publisherID: String!,
}

input UpdateBookMetaInp {
  id: String!,
  name: String,
  abstract: String,
  ISBN: String,
  publisherID: String,
}