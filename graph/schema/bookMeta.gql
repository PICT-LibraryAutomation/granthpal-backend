
extend type Query {
  bookMeta(id: String!): BookMetadata,
  bookMetas: [BookMetadata!]!,
}

extend type Mutation {
  createBookMeta(inp: CreateBookMetaInp!): BookMetadata,
}

type BookMetadata {
  id: String!,
  name: String!,
  abstract: String!,
  ISBN: String!,
  authors: [Author!]!,
  publisherID: String!,
  publisher: Publisher!,
  books: [Book!]!,
}

input CreateBookMetaInp {
  name: String!,
  abstract: String!,
  ISBN: String!,
  authorIDs: [String!]!,
  publisherID: String!,
}